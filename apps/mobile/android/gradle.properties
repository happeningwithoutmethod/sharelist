org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Temporary AGP 9 opt-out until remaining plugins stop applying kotlin-android.
android.builtInKotlin=false
android.newDsl=false

# AVG HTTPS-scan truststore: absolute path is set in settings.gradle.kts from
# android/.certs/avg-truststore.jks (relative systemProp breaks IDE sync CWD).
# See setup-java-trust.ps1 to regenerate the store.
